# Loot default rules — Spanish.
#
# GENERATED by tools/translate. Do not restructure this file by hand; the next
# run rewrites it from internal/rules/default.yaml.
#
# Editing a translation *is* supported, and is the intended way to fix a
# wrong one: an entry you rewrite is left exactly as you wrote it until the
# English title or subtitle it translates changes, at which point it is
# retranslated. (tools/translate records a hash of the English source in
# i18n.lock.json and only touches entries whose hash moved.)
#
# An overlay is the translatable half of default.yaml and nothing else. Each
# entry names a rule from default.yaml and gives its title and subtitle in this
# language; rarity, XP and matching are decisions rather than words and live in
# default.yaml alone.
#
# Every `{{…}}` expression is the English one, because it renders against the
# same event. Reordering them around the sentence is fine and often necessary;
# changing what one says is not.
#
# An entry applies only while the rule it names still has default.yaml's own
# wording. Copy default.yaml, rewrite a title, and that title stays yours in
# every language — see internal/rules/locales.go.

rules:
  - name: revenuecat-churn
    then:
      title: "Suscriptor perdido"
      subtitle: "{{.Kind}}{{if .App}} · {{.App}}{{end}}"
  - name: revenuecat-winback
    then:
      title: "Vuelve un suscriptor"
      subtitle: "{{.App}}"
  - name: revenuecat-annual
    then:
      title: "Nuevo suscriptor anual"
      subtitle: "{{.AmountFmt}}{{if .App}} · {{.App}}{{end}}"
  - name: revenuecat-high-value
    then:
      title: "Nuevo suscriptor"
      subtitle: "{{.AmountFmt}}{{if .App}} · {{.App}}{{end}}"
  - name: revenuecat-purchase
    then:
      title: "Nuevo suscriptor"
      subtitle: "{{.AmountFmt}}{{if .App}} · {{.App}}{{end}}"
  - name: revenuecat-renewal
    then:
      title: "Renovación"
      subtitle: "{{.AmountFmt}}{{if .App}} · {{.App}}{{end}}"
  - name: revenuecat-test
    then:
      title: "Prueba de webhook de RevenueCat recibida"
      subtitle: "la conexión funciona{{if .App}} · {{.App}}{{end}}"
  - name: revenuecat-product-change
    then:
      title: "Plan cambiado"
      subtitle: "{{.App}}"
  - name: sales-day-record
    then:
      title: "El mejor día en {{.SourceName}}"
      subtitle: '{{if gt .Quantity 0}}{{.QuantityFmt}} venta{{if ne .Quantity 1}}s{{end}} · {{.AmountFmt}}{{else if .Payload.downloads}}{{.Payload.downloads}} descarga{{if ne (printf "%v" .Payload.downloads) "1"}}s{{end}}{{else}}un día tranquilo{{end}}{{if .App}} · {{.App}}{{end}}'
  - name: sales-day-big
    then:
      title: '{{if gt .Quantity 0}}{{.QuantityFmt}} venta{{if ne .Quantity 1}}s{{end}} · {{.AmountFmt}}{{else if .Payload.downloads}}{{.Payload.downloads}} descarga{{if ne (printf "%v" .Payload.downloads) "1"}}s{{end}}{{else}}un día tranquilo{{end}}'
      subtitle: "un gran día{{if .App}} para {{.App}}{{end}} en {{.SourceName}}"
  - name: sales-day
    then:
      title: '{{if gt .Quantity 0}}{{.QuantityFmt}} venta{{if ne .Quantity 1}}s{{end}} · {{.AmountFmt}}{{else if .Payload.downloads}}{{.Payload.downloads}} descarga{{if ne (printf "%v" .Payload.downloads) "1"}}s{{end}}{{else}}un día tranquilo{{end}}'
      subtitle: "{{if .App}}{{.App}} · {{end}}{{.SourceName}}"
  - name: flathub-record-day
    then:
      title: "El mejor día en Flathub"
      subtitle: "{{.QuantityFmt}} instalaciones de {{.App}}"
  - name: flathub-installs
    then:
      title: "{{.QuantityFmt}} instalaciones en Flathub"
      subtitle: "{{.App}}"
  - name: installs-day-record
    then:
      title: "Mejor día de instalaciones en {{.SourceName}}"
      subtitle: "{{.QuantityFmt}} instalaciones{{if .App}} · {{.App}}{{end}}"
  - name: installs-day
    then:
      title: "{{.QuantityFmt}} instalaciones en {{.SourceName}}"
      subtitle: "{{.App}}"
  - name: settlement
    then:
      title: "Nuevo asentamiento: {{.Flag}} {{.Country}}"
      subtitle: "primer cliente vía {{.Payload.via}}"
  - name: quest-complete-month
    then:
      title: "Misión completada: {{.Payload.title}}"
      subtitle: "{{.Payload.detail}}"
  - name: quest-complete
    then:
      title: "Misión completada: {{.Payload.title}}"
      subtitle: "{{.Payload.detail}}"
  - name: achievement-legendary
    then:
      title: "Logro: {{.Payload.title}}"
      subtitle: "{{.Payload.description}}"
  - name: achievement-gold
    then:
      title: "Logro: {{.Payload.title}}"
      subtitle: "{{.Payload.description}}"
  - name: achievement-silver
    then:
      title: "Logro: {{.Payload.title}}"
      subtitle: "{{.Payload.description}}"
  - name: achievement
    then:
      title: "Logro: {{.Payload.title}}"
      subtitle: "{{.Payload.description}}"
  - name: boss-spawn
    then:
      title: "Aparece un jefe: {{.Payload.name}}"
      subtitle: "{{.Payload.title}} · {{.Payload.detail}}"
  - name: boss-enrage
    then:
      title: "{{.Payload.name}} se enfurece"
      subtitle: "{{.Payload.detail}}"
  - name: boss-slain-legendary
    then:
      title: "Jefe abatido: {{.Payload.name}}"
      subtitle: "{{.Payload.detail}}"
  - name: boss-slain
    then:
      title: "Jefe abatido: {{.Payload.name}}"
      subtitle: "{{.Payload.detail}}"
  - name: mystery-solved
    then:
      title: "Misterio resuelto"
      subtitle: "{{.Payload.title}}"
  - name: github-release
    then:
      title: "Nueva versión publicada: {{.Payload.tag}}"
      subtitle: "{{.App}}"
  - name: github-star-milestone
    then:
      title: "{{.QuantityFmt}} estrellas en {{.App}}"
      subtitle: "hito"
  - name: github-star
    then:
      title: "Nueva estrella"
      subtitle: "{{.App}}{{if .Payload.user}} · {{.Payload.user}}{{end}}"
  - name: github-fork
    then:
      title: "Nuevo fork"
      subtitle: "{{.App}}"
  - name: github-issue-opened
    then:
      title: "Aparece una misión: #{{.Payload.number}}"
      subtitle: "{{.Payload.title}}"
  - name: github-issue-closed
    then:
      title: "Issue abatido: #{{.Payload.number}}"
      subtitle: "{{.Payload.title}}"
  - name: github-pr-merged
    then:
      title: "PR fusionado: #{{.Payload.number}}"
      subtitle: "{{.Payload.title}}"
  - name: github-pr-opened
    then:
      title: "PR abierto: #{{.Payload.number}}"
      subtitle: "{{.Payload.title}}"
  - name: webhook-legendary
    then:
      title: "{{.Payload.title}}"
      subtitle: "{{.Payload.subtitle}}"
  - name: webhook-epic
    then:
      title: "{{.Payload.title}}"
      subtitle: "{{.Payload.subtitle}}"
  - name: webhook-rare
    then:
      title: "{{.Payload.title}}"
      subtitle: "{{.Payload.subtitle}}"
  - name: webhook-uncommon
    then:
      title: "{{.Payload.title}}"
      subtitle: "{{.Payload.subtitle}}"
  - name: webhook-cursed
    then:
      title: "{{.Payload.title}}"
      subtitle: "{{.Payload.subtitle}}"
  - name: webhook-common
    then:
      title: "{{.Payload.title}}"
      subtitle: "{{.Payload.subtitle}}"
  - name: dev-cursed
    then:
      title: "Un botín maldito"
      subtitle: "evento de prueba sintético"
  - name: dev-legendary
    then:
      title: "Un botín legendario"
      subtitle: "evento de prueba sintético"
  - name: dev-epic
    then:
      title: "Un botín épico"
      subtitle: "evento de prueba sintético"
  - name: dev-rare
    then:
      title: "Un botín raro"
      subtitle: "evento de prueba sintético"
  - name: dev-uncommon
    then:
      title: "Un botín poco común"
      subtitle: "evento de prueba sintético"
  - name: dev-common
    then:
      title: "Un botín común"
      subtitle: "evento de prueba sintético"
fallback:
  then:
    title: "{{.Source}} · {{.Kind}}"
    subtitle: "{{.App}}"
