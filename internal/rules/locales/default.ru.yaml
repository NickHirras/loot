# Loot default rules — Russian.
#
# GENERATED by tools/translate. Do not restructure this file by hand; the next
# run rewrites it from internal/rules/default.yaml.
#
# Editing a translation *is* supported, and is the intended way to fix a
# wrong one: an entry you rewrite is left exactly as you wrote it until the
# English title or subtitle it translates changes, at which point it is
# retranslated. (tools/translate records a hash of the English source in
# i18n.lock.json and only touches entries whose hash moved.)
#
# An overlay is the translatable half of default.yaml and nothing else. Each
# entry names a rule from default.yaml and gives its title and subtitle in this
# language; rarity, XP and matching are decisions rather than words and live in
# default.yaml alone.
#
# Every `{{…}}` expression is the English one, because it renders against the
# same event. Reordering them around the sentence is fine and often necessary;
# changing what one says is not.
#
# An entry applies only while the rule it names still has default.yaml's own
# wording. Copy default.yaml, rewrite a title, and that title stays yours in
# every language — see internal/rules/locales.go.

rules:
  - name: revenuecat-churn
    then:
      title: "Подписчик потерян"
      subtitle: "{{.Kind}}{{if .App}} · {{.App}}{{end}}"
  - name: revenuecat-winback
    then:
      title: "Подписчик вернулся"
      subtitle: "{{.App}}"
  - name: revenuecat-annual
    then:
      title: "Новый годовой подписчик"
      subtitle: "{{.AmountFmt}}{{if .App}} · {{.App}}{{end}}"
  - name: revenuecat-high-value
    then:
      title: "Новый подписчик"
      subtitle: "{{.AmountFmt}}{{if .App}} · {{.App}}{{end}}"
  - name: revenuecat-purchase
    then:
      title: "Новый подписчик"
      subtitle: "{{.AmountFmt}}{{if .App}} · {{.App}}{{end}}"
  - name: revenuecat-renewal
    then:
      title: "Продление"
      subtitle: "{{.AmountFmt}}{{if .App}} · {{.App}}{{end}}"
  - name: revenuecat-test
    then:
      title: "Тестовый вебхук RevenueCat получен"
      subtitle: "связь работает{{if .App}} · {{.App}}{{end}}"
  - name: revenuecat-product-change
    then:
      title: "План изменён"
      subtitle: "{{.App}}"
  - name: sales-day-record
    then:
      title: "Лучший день на {{.SourceName}}"
      subtitle: '{{if gt .Quantity 0}}{{.QuantityFmt}} продаж{{if ne .Quantity 1}}{{end}} · {{.AmountFmt}}{{else if .Payload.downloads}}{{.Payload.downloads}} загрузок{{if ne (printf "%v" .Payload.downloads) "1"}}{{end}}{{else}}тихий день{{end}}{{if .App}} · {{.App}}{{end}}'
  - name: sales-day-big
    then:
      title: '{{if gt .Quantity 0}}{{.QuantityFmt}} продаж{{if ne .Quantity 1}}{{end}} · {{.AmountFmt}}{{else if .Payload.downloads}}{{.Payload.downloads}} загрузок{{if ne (printf "%v" .Payload.downloads) "1"}}{{end}}{{else}}тихий день{{end}}'
      subtitle: "крупный день{{if .App}} для {{.App}}{{end}} на {{.SourceName}}"
  - name: sales-day
    then:
      title: '{{if gt .Quantity 0}}{{.QuantityFmt}} продаж{{if ne .Quantity 1}}{{end}} · {{.AmountFmt}}{{else if .Payload.downloads}}{{.Payload.downloads}} загрузок{{if ne (printf "%v" .Payload.downloads) "1"}}{{end}}{{else}}тихий день{{end}}'
      subtitle: "{{if .App}}{{.App}} · {{end}}{{.SourceName}}"
  - name: flathub-record-day
    then:
      title: "Лучший день на Flathub"
      subtitle: "{{.QuantityFmt}} установок {{.App}}"
  - name: flathub-installs
    then:
      title: "{{.QuantityFmt}} установок на Flathub"
      subtitle: "{{.App}}"
  - name: installs-day-record
    then:
      title: "Лучший день по установкам на {{.SourceName}}"
      subtitle: "{{.QuantityFmt}} установок{{if .App}} · {{.App}}{{end}}"
  - name: installs-day
    then:
      title: "{{.QuantityFmt}} установок на {{.SourceName}}"
      subtitle: "{{.App}}"
  - name: settlement
    then:
      title: "Новое поселение: {{.Flag}} {{.Country}}"
      subtitle: "первый покупатель через {{.Payload.via}}"
  - name: quest-complete-month
    then:
      title: "Квест выполнен: {{.Payload.title}}"
      subtitle: "{{.Payload.detail}}"
  - name: quest-complete
    then:
      title: "Квест выполнен: {{.Payload.title}}"
      subtitle: "{{.Payload.detail}}"
  - name: achievement-legendary
    then:
      title: "Достижение: {{.Payload.title}}"
      subtitle: "{{.Payload.description}}"
  - name: achievement-gold
    then:
      title: "Достижение: {{.Payload.title}}"
      subtitle: "{{.Payload.description}}"
  - name: achievement-silver
    then:
      title: "Достижение: {{.Payload.title}}"
      subtitle: "{{.Payload.description}}"
  - name: achievement
    then:
      title: "Достижение: {{.Payload.title}}"
      subtitle: "{{.Payload.description}}"
  - name: boss-spawn
    then:
      title: "Появился босс: {{.Payload.name}}"
      subtitle: "{{.Payload.title}} · {{.Payload.detail}}"
  - name: boss-enrage
    then:
      title: "{{.Payload.name}} в ярости"
      subtitle: "{{.Payload.detail}}"
  - name: boss-slain-legendary
    then:
      title: "Босс убит: {{.Payload.name}}"
      subtitle: "{{.Payload.detail}}"
  - name: boss-slain
    then:
      title: "Босс убит: {{.Payload.name}}"
      subtitle: "{{.Payload.detail}}"
  - name: mystery-solved
    then:
      title: "Загадка разгадана"
      subtitle: "{{.Payload.title}}"
  - name: github-release
    then:
      title: "Релиз выпущен: {{.Payload.tag}}"
      subtitle: "{{.App}}"
  - name: github-star-milestone
    then:
      title: "{{.QuantityFmt}} звёзд у {{.App}}"
      subtitle: "веха"
  - name: github-star
    then:
      title: "Новый звездочёт"
      subtitle: "{{.App}}{{if .Payload.user}} · {{.Payload.user}}{{end}}"
  - name: github-fork
    then:
      title: "Форк"
      subtitle: "{{.App}}"
  - name: github-issue-opened
    then:
      title: "Появился квест: #{{.Payload.number}}"
      subtitle: "{{.Payload.title}}"
  - name: github-issue-closed
    then:
      title: "Задача убита: #{{.Payload.number}}"
      subtitle: "{{.Payload.title}}"
  - name: github-pr-merged
    then:
      title: "PR влит: #{{.Payload.number}}"
      subtitle: "{{.Payload.title}}"
  - name: github-pr-opened
    then:
      title: "Открыт PR: #{{.Payload.number}}"
      subtitle: "{{.Payload.title}}"
  - name: webhook-legendary
    then:
      title: "{{.Payload.title}}"
      subtitle: "{{.Payload.subtitle}}"
  - name: webhook-epic
    then:
      title: "{{.Payload.title}}"
      subtitle: "{{.Payload.subtitle}}"
  - name: webhook-rare
    then:
      title: "{{.Payload.title}}"
      subtitle: "{{.Payload.subtitle}}"
  - name: webhook-uncommon
    then:
      title: "{{.Payload.title}}"
      subtitle: "{{.Payload.subtitle}}"
  - name: webhook-cursed
    then:
      title: "{{.Payload.title}}"
      subtitle: "{{.Payload.subtitle}}"
  - name: webhook-common
    then:
      title: "{{.Payload.title}}"
      subtitle: "{{.Payload.subtitle}}"
  - name: dev-cursed
    then:
      title: "Проклятый дроп"
      subtitle: "синтетическое тестовое событие"
  - name: dev-legendary
    then:
      title: "Легендарный дроп"
      subtitle: "синтетическое тестовое событие"
  - name: dev-epic
    then:
      title: "Эпический дроп"
      subtitle: "синтетическое тестовое событие"
  - name: dev-rare
    then:
      title: "Редкий дроп"
      subtitle: "синтетическое тестовое событие"
  - name: dev-uncommon
    then:
      title: "Необычный дроп"
      subtitle: "синтетическое тестовое событие"
  - name: dev-common
    then:
      title: "Обычный дроп"
      subtitle: "синтетическое тестовое событие"
fallback:
  then:
    title: "{{.Source}} · {{.Kind}}"
    subtitle: "{{.App}}"
