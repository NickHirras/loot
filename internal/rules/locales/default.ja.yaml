# Loot default rules — Japanese.
#
# GENERATED by tools/translate. Do not restructure this file by hand; the next
# run rewrites it from internal/rules/default.yaml.
#
# Editing a translation *is* supported, and is the intended way to fix a
# wrong one: an entry you rewrite is left exactly as you wrote it until the
# English title or subtitle it translates changes, at which point it is
# retranslated. (tools/translate records a hash of the English source in
# i18n.lock.json and only touches entries whose hash moved.)
#
# An overlay is the translatable half of default.yaml and nothing else. Each
# entry names a rule from default.yaml and gives its title and subtitle in this
# language; rarity, XP and matching are decisions rather than words and live in
# default.yaml alone.
#
# Every `{{…}}` expression is the English one, because it renders against the
# same event. Reordering them around the sentence is fine and often necessary;
# changing what one says is not.
#
# An entry applies only while the rule it names still has default.yaml's own
# wording. Copy default.yaml, rewrite a title, and that title stays yours in
# every language — see internal/rules/locales.go.

rules:
  - name: revenuecat-churn
    then:
      title: "購読者が離脱"
      subtitle: "{{.Kind}}{{if .App}} · {{.App}}{{end}}"
  - name: revenuecat-winback
    then:
      title: "購読者が戻ってきました"
      subtitle: "{{.App}}"
  - name: revenuecat-annual
    then:
      title: "新しい年間購読者"
      subtitle: "{{.AmountFmt}}{{if .App}} · {{.App}}{{end}}"
  - name: revenuecat-high-value
    then:
      title: "新しい購読者"
      subtitle: "{{.AmountFmt}}{{if .App}} · {{.App}}{{end}}"
  - name: revenuecat-purchase
    then:
      title: "新しい購読者"
      subtitle: "{{.AmountFmt}}{{if .App}} · {{.App}}{{end}}"
  - name: revenuecat-renewal
    then:
      title: "更新"
      subtitle: "{{.AmountFmt}}{{if .App}} · {{.App}}{{end}}"
  - name: revenuecat-test
    then:
      title: "RevenueCatのWebhookテストを受信"
      subtitle: "配線は正常です{{if .App}} · {{.App}}{{end}}"
  - name: revenuecat-product-change
    then:
      title: "プラン変更"
      subtitle: "{{.App}}"
  - name: sales-day-record
    then:
      title: "{{.SourceName}}で過去最高の日"
      subtitle: '{{if gt .Quantity 0}}{{.QuantityFmt}}件の販売{{if ne .Quantity 1}}{{end}} · {{.AmountFmt}}{{else if .Payload.downloads}}{{.Payload.downloads}}件のダウンロード{{if ne (printf "%v" .Payload.downloads) "1"}}{{end}}{{else}}静かな一日{{end}}{{if .App}} · {{.App}}{{end}}'
  - name: sales-day-big
    then:
      title: '{{if gt .Quantity 0}}{{.QuantityFmt}}件の販売{{if ne .Quantity 1}}{{end}} · {{.AmountFmt}}{{else if .Payload.downloads}}{{.Payload.downloads}}件のダウンロード{{if ne (printf "%v" .Payload.downloads) "1"}}{{end}}{{else}}静かな一日{{end}}'
      subtitle: "{{if .App}}{{.App}}の{{end}}{{.SourceName}}で大きな一日"
  - name: sales-day
    then:
      title: '{{if gt .Quantity 0}}{{.QuantityFmt}}件の販売{{if ne .Quantity 1}}{{end}} · {{.AmountFmt}}{{else if .Payload.downloads}}{{.Payload.downloads}}件のダウンロード{{if ne (printf "%v" .Payload.downloads) "1"}}{{end}}{{else}}静かな一日{{end}}'
      subtitle: "{{if .App}}{{.App}} · {{end}}{{.SourceName}}"
  - name: flathub-record-day
    then:
      title: "Flathubで過去最高の日"
      subtitle: "{{.App}}が{{.QuantityFmt}}インストール"
  - name: flathub-installs
    then:
      title: "Flathubで{{.QuantityFmt}}インストール"
      subtitle: "{{.App}}"
  - name: installs-day-record
    then:
      title: "{{.SourceName}}で過去最高のインストール日"
      subtitle: "{{.QuantityFmt}}インストール{{if .App}} · {{.App}}{{end}}"
  - name: installs-day
    then:
      title: "{{.SourceName}}で{{.QuantityFmt}}インストール"
      subtitle: "{{.App}}"
  - name: settlement
    then:
      title: "新しい開拓地: {{.Flag}} {{.Country}}"
      subtitle: "{{.Payload.via}}経由で最初の顧客"
  - name: quest-complete-month
    then:
      title: "クエスト達成: {{.Payload.title}}"
      subtitle: "{{.Payload.detail}}"
  - name: quest-complete
    then:
      title: "クエスト達成: {{.Payload.title}}"
      subtitle: "{{.Payload.detail}}"
  - name: achievement-legendary
    then:
      title: "実績: {{.Payload.title}}"
      subtitle: "{{.Payload.description}}"
  - name: achievement-gold
    then:
      title: "実績: {{.Payload.title}}"
      subtitle: "{{.Payload.description}}"
  - name: achievement-silver
    then:
      title: "実績: {{.Payload.title}}"
      subtitle: "{{.Payload.description}}"
  - name: achievement
    then:
      title: "実績: {{.Payload.title}}"
      subtitle: "{{.Payload.description}}"
  - name: boss-spawn
    then:
      title: "ボス出現: {{.Payload.name}}"
      subtitle: "{{.Payload.title}} · {{.Payload.detail}}"
  - name: boss-enrage
    then:
      title: "{{.Payload.name}}が激昂"
      subtitle: "{{.Payload.detail}}"
  - name: boss-slain-legendary
    then:
      title: "ボス討伐: {{.Payload.name}}"
      subtitle: "{{.Payload.detail}}"
  - name: boss-slain
    then:
      title: "ボス討伐: {{.Payload.name}}"
      subtitle: "{{.Payload.detail}}"
  - name: mystery-solved
    then:
      title: "謎を解明"
      subtitle: "{{.Payload.title}}"
  - name: github-release
    then:
      title: "リリース公開: {{.Payload.tag}}"
      subtitle: "{{.App}}"
  - name: github-star-milestone
    then:
      title: "{{.App}}が{{.QuantityFmt}}スター"
      subtitle: "マイルストーン"
  - name: github-star
    then:
      title: "新しいスターゲイザー"
      subtitle: "{{.App}}{{if .Payload.user}} · {{.Payload.user}}{{end}}"
  - name: github-fork
    then:
      title: "フォークされました"
      subtitle: "{{.App}}"
  - name: github-issue-opened
    then:
      title: "クエスト出現: #{{.Payload.number}}"
      subtitle: "{{.Payload.title}}"
  - name: github-issue-closed
    then:
      title: "Issue討伐: #{{.Payload.number}}"
      subtitle: "{{.Payload.title}}"
  - name: github-pr-merged
    then:
      title: "PRマージ: #{{.Payload.number}}"
      subtitle: "{{.Payload.title}}"
  - name: github-pr-opened
    then:
      title: "PRオープン: #{{.Payload.number}}"
      subtitle: "{{.Payload.title}}"
  - name: webhook-legendary
    then:
      title: "{{.Payload.title}}"
      subtitle: "{{.Payload.subtitle}}"
  - name: webhook-epic
    then:
      title: "{{.Payload.title}}"
      subtitle: "{{.Payload.subtitle}}"
  - name: webhook-rare
    then:
      title: "{{.Payload.title}}"
      subtitle: "{{.Payload.subtitle}}"
  - name: webhook-uncommon
    then:
      title: "{{.Payload.title}}"
      subtitle: "{{.Payload.subtitle}}"
  - name: webhook-cursed
    then:
      title: "{{.Payload.title}}"
      subtitle: "{{.Payload.subtitle}}"
  - name: webhook-common
    then:
      title: "{{.Payload.title}}"
      subtitle: "{{.Payload.subtitle}}"
  - name: dev-cursed
    then:
      title: "呪いのドロップ"
      subtitle: "合成テストイベント"
  - name: dev-legendary
    then:
      title: "レジェンダリーのドロップ"
      subtitle: "合成テストイベント"
  - name: dev-epic
    then:
      title: "エピックのドロップ"
      subtitle: "合成テストイベント"
  - name: dev-rare
    then:
      title: "レアのドロップ"
      subtitle: "合成テストイベント"
  - name: dev-uncommon
    then:
      title: "アンコモンのドロップ"
      subtitle: "合成テストイベント"
  - name: dev-common
    then:
      title: "コモンのドロップ"
      subtitle: "合成テストイベント"
fallback:
  then:
    title: "{{.Source}} · {{.Kind}}"
    subtitle: "{{.App}}"
