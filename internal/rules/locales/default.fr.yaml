# Loot default rules — French.
#
# GENERATED by tools/translate. Do not restructure this file by hand; the next
# run rewrites it from internal/rules/default.yaml.
#
# Editing a translation *is* supported, and is the intended way to fix a
# wrong one: an entry you rewrite is left exactly as you wrote it until the
# English title or subtitle it translates changes, at which point it is
# retranslated. (tools/translate records a hash of the English source in
# i18n.lock.json and only touches entries whose hash moved.)
#
# An overlay is the translatable half of default.yaml and nothing else. Each
# entry names a rule from default.yaml and gives its title and subtitle in this
# language; rarity, XP and matching are decisions rather than words and live in
# default.yaml alone.
#
# Every `{{…}}` expression is the English one, because it renders against the
# same event. Reordering them around the sentence is fine and often necessary;
# changing what one says is not.
#
# An entry applies only while the rule it names still has default.yaml's own
# wording. Copy default.yaml, rewrite a title, and that title stays yours in
# every language — see internal/rules/locales.go.

rules:
  - name: revenuecat-churn
    then:
      title: "Abonné perdu"
      subtitle: "{{.Kind}}{{if .App}} · {{.App}}{{end}}"
  - name: revenuecat-winback
    then:
      title: "Un abonné revient"
      subtitle: "{{.App}}"
  - name: revenuecat-annual
    then:
      title: "Nouvel abonné annuel"
      subtitle: "{{.AmountFmt}}{{if .App}} · {{.App}}{{end}}"
  - name: revenuecat-high-value
    then:
      title: "Nouvel abonné"
      subtitle: "{{.AmountFmt}}{{if .App}} · {{.App}}{{end}}"
  - name: revenuecat-purchase
    then:
      title: "Nouvel abonné"
      subtitle: "{{.AmountFmt}}{{if .App}} · {{.App}}{{end}}"
  - name: revenuecat-renewal
    then:
      title: "Renouvellement"
      subtitle: "{{.AmountFmt}}{{if .App}} · {{.App}}{{end}}"
  - name: revenuecat-test
    then:
      title: "Test du webhook RevenueCat reçu"
      subtitle: "le câblage fonctionne{{if .App}} · {{.App}}{{end}}"
  - name: revenuecat-product-change
    then:
      title: "Formule changée"
      subtitle: "{{.App}}"
  - name: sales-day-record
    then:
      title: "Meilleur jour sur {{.SourceName}}"
      subtitle: '{{if gt .Quantity 0}}{{.QuantityFmt}} vente{{if ne .Quantity 1}}s{{end}} · {{.AmountFmt}}{{else if .Payload.downloads}}{{.Payload.downloads}} téléchargement{{if ne (printf "%v" .Payload.downloads) "1"}}s{{end}}{{else}}une journée calme{{end}}{{if .App}} · {{.App}}{{end}}'
  - name: sales-day-big
    then:
      title: '{{if gt .Quantity 0}}{{.QuantityFmt}} vente{{if ne .Quantity 1}}s{{end}} · {{.AmountFmt}}{{else if .Payload.downloads}}{{.Payload.downloads}} téléchargement{{if ne (printf "%v" .Payload.downloads) "1"}}s{{end}}{{else}}une journée calme{{end}}'
      subtitle: "une grosse journée{{if .App}} pour {{.App}}{{end}} sur {{.SourceName}}"
  - name: sales-day
    then:
      title: '{{if gt .Quantity 0}}{{.QuantityFmt}} vente{{if ne .Quantity 1}}s{{end}} · {{.AmountFmt}}{{else if .Payload.downloads}}{{.Payload.downloads}} téléchargement{{if ne (printf "%v" .Payload.downloads) "1"}}s{{end}}{{else}}une journée calme{{end}}'
      subtitle: "{{if .App}}{{.App}} · {{end}}{{.SourceName}}"
  - name: flathub-record-day
    then:
      title: "Meilleur jour sur Flathub"
      subtitle: "{{.QuantityFmt}} installations de {{.App}}"
  - name: flathub-installs
    then:
      title: "{{.QuantityFmt}} installations sur Flathub"
      subtitle: "{{.App}}"
  - name: installs-day-record
    then:
      title: "Meilleur jour en installations sur {{.SourceName}}"
      subtitle: "{{.QuantityFmt}} installations{{if .App}} · {{.App}}{{end}}"
  - name: installs-day
    then:
      title: "{{.QuantityFmt}} installations sur {{.SourceName}}"
      subtitle: "{{.App}}"
  - name: settlement
    then:
      title: "Nouvelle colonie : {{.Flag}} {{.Country}}"
      subtitle: "premier client via {{.Payload.via}}"
  - name: quest-complete-month
    then:
      title: "Quête accomplie : {{.Payload.title}}"
      subtitle: "{{.Payload.detail}}"
  - name: quest-complete
    then:
      title: "Quête accomplie : {{.Payload.title}}"
      subtitle: "{{.Payload.detail}}"
  - name: achievement-legendary
    then:
      title: "Succès : {{.Payload.title}}"
      subtitle: "{{.Payload.description}}"
  - name: achievement-gold
    then:
      title: "Succès : {{.Payload.title}}"
      subtitle: "{{.Payload.description}}"
  - name: achievement-silver
    then:
      title: "Succès : {{.Payload.title}}"
      subtitle: "{{.Payload.description}}"
  - name: achievement
    then:
      title: "Succès : {{.Payload.title}}"
      subtitle: "{{.Payload.description}}"
  - name: boss-spawn
    then:
      title: "Un boss apparaît : {{.Payload.name}}"
      subtitle: "{{.Payload.title}} · {{.Payload.detail}}"
  - name: boss-enrage
    then:
      title: "{{.Payload.name}} entre en rage"
      subtitle: "{{.Payload.detail}}"
  - name: boss-slain-legendary
    then:
      title: "Boss vaincu : {{.Payload.name}}"
      subtitle: "{{.Payload.detail}}"
  - name: boss-slain
    then:
      title: "Boss vaincu : {{.Payload.name}}"
      subtitle: "{{.Payload.detail}}"
  - name: mystery-solved
    then:
      title: "Mystère résolu"
      subtitle: "{{.Payload.title}}"
  - name: github-release
    then:
      title: "Version publiée : {{.Payload.tag}}"
      subtitle: "{{.App}}"
  - name: github-star-milestone
    then:
      title: "{{.QuantityFmt}} étoiles sur {{.App}}"
      subtitle: "jalon"
  - name: github-star
    then:
      title: "Nouvelle étoile"
      subtitle: "{{.App}}{{if .Payload.user}} · {{.Payload.user}}{{end}}"
  - name: github-fork
    then:
      title: "Nouveau fork"
      subtitle: "{{.App}}"
  - name: github-issue-opened
    then:
      title: "Une quête apparaît : #{{.Payload.number}}"
      subtitle: "{{.Payload.title}}"
  - name: github-issue-closed
    then:
      title: "Ticket vaincu : #{{.Payload.number}}"
      subtitle: "{{.Payload.title}}"
  - name: github-pr-merged
    then:
      title: "PR fusionnée : #{{.Payload.number}}"
      subtitle: "{{.Payload.title}}"
  - name: github-pr-opened
    then:
      title: "PR ouverte : #{{.Payload.number}}"
      subtitle: "{{.Payload.title}}"
  - name: webhook-legendary
    then:
      title: "{{.Payload.title}}"
      subtitle: "{{.Payload.subtitle}}"
  - name: webhook-epic
    then:
      title: "{{.Payload.title}}"
      subtitle: "{{.Payload.subtitle}}"
  - name: webhook-rare
    then:
      title: "{{.Payload.title}}"
      subtitle: "{{.Payload.subtitle}}"
  - name: webhook-uncommon
    then:
      title: "{{.Payload.title}}"
      subtitle: "{{.Payload.subtitle}}"
  - name: webhook-cursed
    then:
      title: "{{.Payload.title}}"
      subtitle: "{{.Payload.subtitle}}"
  - name: webhook-common
    then:
      title: "{{.Payload.title}}"
      subtitle: "{{.Payload.subtitle}}"
  - name: dev-cursed
    then:
      title: "Un butin maudit"
      subtitle: "événement de test synthétique"
  - name: dev-legendary
    then:
      title: "Un butin légendaire"
      subtitle: "événement de test synthétique"
  - name: dev-epic
    then:
      title: "Un butin épique"
      subtitle: "événement de test synthétique"
  - name: dev-rare
    then:
      title: "Un butin rare"
      subtitle: "événement de test synthétique"
  - name: dev-uncommon
    then:
      title: "Un butin peu commun"
      subtitle: "événement de test synthétique"
  - name: dev-common
    then:
      title: "Un butin commun"
      subtitle: "événement de test synthétique"
fallback:
  then:
    title: "{{.Source}} · {{.Kind}}"
    subtitle: "{{.App}}"
