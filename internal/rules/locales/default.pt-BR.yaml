# Loot default rules — Brazilian Portuguese.
#
# GENERATED by tools/translate. Do not restructure this file by hand; the next
# run rewrites it from internal/rules/default.yaml.
#
# Editing a translation *is* supported, and is the intended way to fix a
# wrong one: an entry you rewrite is left exactly as you wrote it until the
# English title or subtitle it translates changes, at which point it is
# retranslated. (tools/translate records a hash of the English source in
# i18n.lock.json and only touches entries whose hash moved.)
#
# An overlay is the translatable half of default.yaml and nothing else. Each
# entry names a rule from default.yaml and gives its title and subtitle in this
# language; rarity, XP and matching are decisions rather than words and live in
# default.yaml alone.
#
# Every `{{…}}` expression is the English one, because it renders against the
# same event. Reordering them around the sentence is fine and often necessary;
# changing what one says is not.
#
# An entry applies only while the rule it names still has default.yaml's own
# wording. Copy default.yaml, rewrite a title, and that title stays yours in
# every language — see internal/rules/locales.go.

rules:
  - name: revenuecat-churn
    then:
      title: "Assinante perdido"
      subtitle: "{{.Kind}}{{if .App}} · {{.App}}{{end}}"
  - name: revenuecat-winback
    then:
      title: "Um assinante voltou"
      subtitle: "{{.App}}"
  - name: revenuecat-annual
    then:
      title: "Novo assinante anual"
      subtitle: "{{.AmountFmt}}{{if .App}} · {{.App}}{{end}}"
  - name: revenuecat-high-value
    then:
      title: "Novo assinante"
      subtitle: "{{.AmountFmt}}{{if .App}} · {{.App}}{{end}}"
  - name: revenuecat-purchase
    then:
      title: "Novo assinante"
      subtitle: "{{.AmountFmt}}{{if .App}} · {{.App}}{{end}}"
  - name: revenuecat-renewal
    then:
      title: "Renovação"
      subtitle: "{{.AmountFmt}}{{if .App}} · {{.App}}{{end}}"
  - name: revenuecat-test
    then:
      title: "Teste de webhook do RevenueCat recebido"
      subtitle: "a ligação funciona{{if .App}} · {{.App}}{{end}}"
  - name: revenuecat-product-change
    then:
      title: "Plano alterado"
      subtitle: "{{.App}}"
  - name: sales-day-record
    then:
      title: "Melhor dia de todos em {{.SourceName}}"
      subtitle: '{{if gt .Quantity 0}}{{.QuantityFmt}} venda{{if ne .Quantity 1}}s{{end}} · {{.AmountFmt}}{{else if .Payload.downloads}}{{.Payload.downloads}} download{{if ne (printf "%v" .Payload.downloads) "1"}}s{{end}}{{else}}um dia quieto{{end}}{{if .App}} · {{.App}}{{end}}'
  - name: sales-day-big
    then:
      title: '{{if gt .Quantity 0}}{{.QuantityFmt}} venda{{if ne .Quantity 1}}s{{end}} · {{.AmountFmt}}{{else if .Payload.downloads}}{{.Payload.downloads}} download{{if ne (printf "%v" .Payload.downloads) "1"}}s{{end}}{{else}}um dia quieto{{end}}'
      subtitle: "um dia grande{{if .App}} para {{.App}}{{end}} em {{.SourceName}}"
  - name: sales-day
    then:
      title: '{{if gt .Quantity 0}}{{.QuantityFmt}} venda{{if ne .Quantity 1}}s{{end}} · {{.AmountFmt}}{{else if .Payload.downloads}}{{.Payload.downloads}} download{{if ne (printf "%v" .Payload.downloads) "1"}}s{{end}}{{else}}um dia quieto{{end}}'
      subtitle: "{{if .App}}{{.App}} · {{end}}{{.SourceName}}"
  - name: flathub-record-day
    then:
      title: "Melhor dia de todos no Flathub"
      subtitle: "{{.QuantityFmt}} instalações de {{.App}}"
  - name: flathub-installs
    then:
      title: "{{.QuantityFmt}} instalações no Flathub"
      subtitle: "{{.App}}"
  - name: installs-day-record
    then:
      title: "Melhor dia de instalações em {{.SourceName}}"
      subtitle: "{{.QuantityFmt}} instalações{{if .App}} · {{.App}}{{end}}"
  - name: installs-day
    then:
      title: "{{.QuantityFmt}} instalações em {{.SourceName}}"
      subtitle: "{{.App}}"
  - name: settlement
    then:
      title: "Novo assentamento: {{.Flag}} {{.Country}}"
      subtitle: "primeiro cliente via {{.Payload.via}}"
  - name: quest-complete-month
    then:
      title: "Missão concluída: {{.Payload.title}}"
      subtitle: "{{.Payload.detail}}"
  - name: quest-complete
    then:
      title: "Missão concluída: {{.Payload.title}}"
      subtitle: "{{.Payload.detail}}"
  - name: achievement-legendary
    then:
      title: "Conquista: {{.Payload.title}}"
      subtitle: "{{.Payload.description}}"
  - name: achievement-gold
    then:
      title: "Conquista: {{.Payload.title}}"
      subtitle: "{{.Payload.description}}"
  - name: achievement-silver
    then:
      title: "Conquista: {{.Payload.title}}"
      subtitle: "{{.Payload.description}}"
  - name: achievement
    then:
      title: "Conquista: {{.Payload.title}}"
      subtitle: "{{.Payload.description}}"
  - name: boss-spawn
    then:
      title: "Um boss aparece: {{.Payload.name}}"
      subtitle: "{{.Payload.title}} · {{.Payload.detail}}"
  - name: boss-enrage
    then:
      title: "{{.Payload.name}} se enfurece"
      subtitle: "{{.Payload.detail}}"
  - name: boss-slain-legendary
    then:
      title: "Boss abatido: {{.Payload.name}}"
      subtitle: "{{.Payload.detail}}"
  - name: boss-slain
    then:
      title: "Boss abatido: {{.Payload.name}}"
      subtitle: "{{.Payload.detail}}"
  - name: mystery-solved
    then:
      title: "Mistério resolvido"
      subtitle: "{{.Payload.title}}"
  - name: github-release
    then:
      title: "Versão lançada: {{.Payload.tag}}"
      subtitle: "{{.App}}"
  - name: github-star-milestone
    then:
      title: "{{.QuantityFmt}} estrelas em {{.App}}"
      subtitle: "marco"
  - name: github-star
    then:
      title: "Nova estrela"
      subtitle: "{{.App}}{{if .Payload.user}} · {{.Payload.user}}{{end}}"
  - name: github-fork
    then:
      title: "Forkado"
      subtitle: "{{.App}}"
  - name: github-issue-opened
    then:
      title: "Uma missão aparece: #{{.Payload.number}}"
      subtitle: "{{.Payload.title}}"
  - name: github-issue-closed
    then:
      title: "Issue abatida: #{{.Payload.number}}"
      subtitle: "{{.Payload.title}}"
  - name: github-pr-merged
    then:
      title: "PR mergeado: #{{.Payload.number}}"
      subtitle: "{{.Payload.title}}"
  - name: github-pr-opened
    then:
      title: "PR aberto: #{{.Payload.number}}"
      subtitle: "{{.Payload.title}}"
  - name: webhook-legendary
    then:
      title: "{{.Payload.title}}"
      subtitle: "{{.Payload.subtitle}}"
  - name: webhook-epic
    then:
      title: "{{.Payload.title}}"
      subtitle: "{{.Payload.subtitle}}"
  - name: webhook-rare
    then:
      title: "{{.Payload.title}}"
      subtitle: "{{.Payload.subtitle}}"
  - name: webhook-uncommon
    then:
      title: "{{.Payload.title}}"
      subtitle: "{{.Payload.subtitle}}"
  - name: webhook-cursed
    then:
      title: "{{.Payload.title}}"
      subtitle: "{{.Payload.subtitle}}"
  - name: webhook-common
    then:
      title: "{{.Payload.title}}"
      subtitle: "{{.Payload.subtitle}}"
  - name: dev-cursed
    then:
      title: "Um drop amaldiçoado"
      subtitle: "evento de teste sintético"
  - name: dev-legendary
    then:
      title: "Um drop lendário"
      subtitle: "evento de teste sintético"
  - name: dev-epic
    then:
      title: "Um drop épico"
      subtitle: "evento de teste sintético"
  - name: dev-rare
    then:
      title: "Um drop raro"
      subtitle: "evento de teste sintético"
  - name: dev-uncommon
    then:
      title: "Um drop incomum"
      subtitle: "evento de teste sintético"
  - name: dev-common
    then:
      title: "Um drop comum"
      subtitle: "evento de teste sintético"
fallback:
  then:
    title: "{{.Source}} · {{.Kind}}"
    subtitle: "{{.App}}"
