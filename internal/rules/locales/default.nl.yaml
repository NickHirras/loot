# Loot default rules — Dutch.
#
# GENERATED by tools/translate. Do not restructure this file by hand; the next
# run rewrites it from internal/rules/default.yaml.
#
# Editing a translation *is* supported, and is the intended way to fix a
# wrong one: an entry you rewrite is left exactly as you wrote it until the
# English title or subtitle it translates changes, at which point it is
# retranslated. (tools/translate records a hash of the English source in
# i18n.lock.json and only touches entries whose hash moved.)
#
# An overlay is the translatable half of default.yaml and nothing else. Each
# entry names a rule from default.yaml and gives its title and subtitle in this
# language; rarity, XP and matching are decisions rather than words and live in
# default.yaml alone.
#
# Every `{{…}}` expression is the English one, because it renders against the
# same event. Reordering them around the sentence is fine and often necessary;
# changing what one says is not.
#
# An entry applies only while the rule it names still has default.yaml's own
# wording. Copy default.yaml, rewrite a title, and that title stays yours in
# every language — see internal/rules/locales.go.

rules:
  - name: revenuecat-churn
    then:
      title: "Abonnee verloren"
      subtitle: "{{.Kind}}{{if .App}} · {{.App}}{{end}}"
  - name: revenuecat-winback
    then:
      title: "Een abonnee keert terug"
      subtitle: "{{.App}}"
  - name: revenuecat-annual
    then:
      title: "Nieuwe jaarabonnee"
      subtitle: "{{.AmountFmt}}{{if .App}} · {{.App}}{{end}}"
  - name: revenuecat-high-value
    then:
      title: "Nieuwe abonnee"
      subtitle: "{{.AmountFmt}}{{if .App}} · {{.App}}{{end}}"
  - name: revenuecat-purchase
    then:
      title: "Nieuwe abonnee"
      subtitle: "{{.AmountFmt}}{{if .App}} · {{.App}}{{end}}"
  - name: revenuecat-renewal
    then:
      title: "Verlenging"
      subtitle: "{{.AmountFmt}}{{if .App}} · {{.App}}{{end}}"
  - name: revenuecat-test
    then:
      title: "RevenueCat-webhooktest ontvangen"
      subtitle: "de bedrading werkt{{if .App}} · {{.App}}{{end}}"
  - name: revenuecat-product-change
    then:
      title: "Plan gewijzigd"
      subtitle: "{{.App}}"
  - name: sales-day-record
    then:
      title: "Beste dag ooit op {{.SourceName}}"
      subtitle: '{{if gt .Quantity 0}}{{.QuantityFmt}} verkoop{{if ne .Quantity 1}}en{{end}} · {{.AmountFmt}}{{else if .Payload.downloads}}{{.Payload.downloads}} download{{if ne (printf "%v" .Payload.downloads) "1"}}s{{end}}{{else}}een rustige dag{{end}}{{if .App}} · {{.App}}{{end}}'
  - name: sales-day-big
    then:
      title: '{{if gt .Quantity 0}}{{.QuantityFmt}} verkoop{{if ne .Quantity 1}}en{{end}} · {{.AmountFmt}}{{else if .Payload.downloads}}{{.Payload.downloads}} download{{if ne (printf "%v" .Payload.downloads) "1"}}s{{end}}{{else}}een rustige dag{{end}}'
      subtitle: "een grote dag{{if .App}} voor {{.App}}{{end}} op {{.SourceName}}"
  - name: sales-day
    then:
      title: '{{if gt .Quantity 0}}{{.QuantityFmt}} verkoop{{if ne .Quantity 1}}en{{end}} · {{.AmountFmt}}{{else if .Payload.downloads}}{{.Payload.downloads}} download{{if ne (printf "%v" .Payload.downloads) "1"}}s{{end}}{{else}}een rustige dag{{end}}'
      subtitle: "{{if .App}}{{.App}} · {{end}}{{.SourceName}}"
  - name: flathub-record-day
    then:
      title: "Beste dag ooit op Flathub"
      subtitle: "{{.QuantityFmt}} installaties van {{.App}}"
  - name: flathub-installs
    then:
      title: "{{.QuantityFmt}} installaties op Flathub"
      subtitle: "{{.App}}"
  - name: installs-day-record
    then:
      title: "Beste installatiedag op {{.SourceName}}"
      subtitle: "{{.QuantityFmt}} installaties{{if .App}} · {{.App}}{{end}}"
  - name: installs-day
    then:
      title: "{{.QuantityFmt}} installaties op {{.SourceName}}"
      subtitle: "{{.App}}"
  - name: settlement
    then:
      title: "Nieuwe nederzetting: {{.Flag}} {{.Country}}"
      subtitle: "eerste klant via {{.Payload.via}}"
  - name: quest-complete-month
    then:
      title: "Quest voltooid: {{.Payload.title}}"
      subtitle: "{{.Payload.detail}}"
  - name: quest-complete
    then:
      title: "Quest voltooid: {{.Payload.title}}"
      subtitle: "{{.Payload.detail}}"
  - name: achievement-legendary
    then:
      title: "Prestatie: {{.Payload.title}}"
      subtitle: "{{.Payload.description}}"
  - name: achievement-gold
    then:
      title: "Prestatie: {{.Payload.title}}"
      subtitle: "{{.Payload.description}}"
  - name: achievement-silver
    then:
      title: "Prestatie: {{.Payload.title}}"
      subtitle: "{{.Payload.description}}"
  - name: achievement
    then:
      title: "Prestatie: {{.Payload.title}}"
      subtitle: "{{.Payload.description}}"
  - name: boss-spawn
    then:
      title: "Er verschijnt een boss: {{.Payload.name}}"
      subtitle: "{{.Payload.title}} · {{.Payload.detail}}"
  - name: boss-enrage
    then:
      title: "{{.Payload.name}} wordt razend"
      subtitle: "{{.Payload.detail}}"
  - name: boss-slain-legendary
    then:
      title: "Boss verslagen: {{.Payload.name}}"
      subtitle: "{{.Payload.detail}}"
  - name: boss-slain
    then:
      title: "Boss verslagen: {{.Payload.name}}"
      subtitle: "{{.Payload.detail}}"
  - name: mystery-solved
    then:
      title: "Mysterie opgelost"
      subtitle: "{{.Payload.title}}"
  - name: github-release
    then:
      title: "Release uitgebracht: {{.Payload.tag}}"
      subtitle: "{{.App}}"
  - name: github-star-milestone
    then:
      title: "{{.QuantityFmt}} sterren op {{.App}}"
      subtitle: "mijlpaal"
  - name: github-star
    then:
      title: "Nieuwe sterrenkijker"
      subtitle: "{{.App}}{{if .Payload.user}} · {{.Payload.user}}{{end}}"
  - name: github-fork
    then:
      title: "Geforkt"
      subtitle: "{{.App}}"
  - name: github-issue-opened
    then:
      title: "Er verschijnt een quest: #{{.Payload.number}}"
      subtitle: "{{.Payload.title}}"
  - name: github-issue-closed
    then:
      title: "Issue verslagen: #{{.Payload.number}}"
      subtitle: "{{.Payload.title}}"
  - name: github-pr-merged
    then:
      title: "PR gemerged: #{{.Payload.number}}"
      subtitle: "{{.Payload.title}}"
  - name: github-pr-opened
    then:
      title: "PR geopend: #{{.Payload.number}}"
      subtitle: "{{.Payload.title}}"
  - name: webhook-legendary
    then:
      title: "{{.Payload.title}}"
      subtitle: "{{.Payload.subtitle}}"
  - name: webhook-epic
    then:
      title: "{{.Payload.title}}"
      subtitle: "{{.Payload.subtitle}}"
  - name: webhook-rare
    then:
      title: "{{.Payload.title}}"
      subtitle: "{{.Payload.subtitle}}"
  - name: webhook-uncommon
    then:
      title: "{{.Payload.title}}"
      subtitle: "{{.Payload.subtitle}}"
  - name: webhook-cursed
    then:
      title: "{{.Payload.title}}"
      subtitle: "{{.Payload.subtitle}}"
  - name: webhook-common
    then:
      title: "{{.Payload.title}}"
      subtitle: "{{.Payload.subtitle}}"
  - name: dev-cursed
    then:
      title: "Een vervloekte drop"
      subtitle: "synthetische testgebeurtenis"
  - name: dev-legendary
    then:
      title: "Een legendarische drop"
      subtitle: "synthetische testgebeurtenis"
  - name: dev-epic
    then:
      title: "Een epische drop"
      subtitle: "synthetische testgebeurtenis"
  - name: dev-rare
    then:
      title: "Een zeldzame drop"
      subtitle: "synthetische testgebeurtenis"
  - name: dev-uncommon
    then:
      title: "Een ongewone drop"
      subtitle: "synthetische testgebeurtenis"
  - name: dev-common
    then:
      title: "Een gewone drop"
      subtitle: "synthetische testgebeurtenis"
fallback:
  then:
    title: "{{.Source}} · {{.Kind}}"
    subtitle: "{{.App}}"
