# Loot default rules — Simplified Chinese.
#
# GENERATED by tools/translate. Do not restructure this file by hand; the next
# run rewrites it from internal/rules/default.yaml.
#
# Editing a translation *is* supported, and is the intended way to fix a
# wrong one: an entry you rewrite is left exactly as you wrote it until the
# English title or subtitle it translates changes, at which point it is
# retranslated. (tools/translate records a hash of the English source in
# i18n.lock.json and only touches entries whose hash moved.)
#
# An overlay is the translatable half of default.yaml and nothing else. Each
# entry names a rule from default.yaml and gives its title and subtitle in this
# language; rarity, XP and matching are decisions rather than words and live in
# default.yaml alone.
#
# Every `{{…}}` expression is the English one, because it renders against the
# same event. Reordering them around the sentence is fine and often necessary;
# changing what one says is not.
#
# An entry applies only while the rule it names still has default.yaml's own
# wording. Copy default.yaml, rewrite a title, and that title stays yours in
# every language — see internal/rules/locales.go.

rules:
  - name: revenuecat-churn
    then:
      title: "失去一位订阅者"
      subtitle: "{{.Kind}}{{if .App}} · {{.App}}{{end}}"
  - name: revenuecat-winback
    then:
      title: "一位订阅者回归"
      subtitle: "{{.App}}"
  - name: revenuecat-annual
    then:
      title: "新的年度订阅者"
      subtitle: "{{.AmountFmt}}{{if .App}} · {{.App}}{{end}}"
  - name: revenuecat-high-value
    then:
      title: "新订阅者"
      subtitle: "{{.AmountFmt}}{{if .App}} · {{.App}}{{end}}"
  - name: revenuecat-purchase
    then:
      title: "新订阅者"
      subtitle: "{{.AmountFmt}}{{if .App}} · {{.App}}{{end}}"
  - name: revenuecat-renewal
    then:
      title: "续订"
      subtitle: "{{.AmountFmt}}{{if .App}} · {{.App}}{{end}}"
  - name: revenuecat-test
    then:
      title: "已收到 RevenueCat webhook 测试"
      subtitle: "接线正常{{if .App}} · {{.App}}{{end}}"
  - name: revenuecat-product-change
    then:
      title: "套餐已变更"
      subtitle: "{{.App}}"
  - name: sales-day-record
    then:
      title: "{{.SourceName}} 史上最佳一天"
      subtitle: '{{if gt .Quantity 0}}{{.QuantityFmt}} 笔销售{{if ne .Quantity 1}}{{end}} · {{.AmountFmt}}{{else if .Payload.downloads}}{{.Payload.downloads}} 次下载{{if ne (printf "%v" .Payload.downloads) "1"}}{{end}}{{else}}平静的一天{{end}}{{if .App}} · {{.App}}{{end}}'
  - name: sales-day-big
    then:
      title: '{{if gt .Quantity 0}}{{.QuantityFmt}} 笔销售{{if ne .Quantity 1}}{{end}} · {{.AmountFmt}}{{else if .Payload.downloads}}{{.Payload.downloads}} 次下载{{if ne (printf "%v" .Payload.downloads) "1"}}{{end}}{{else}}平静的一天{{end}}'
      subtitle: "{{.SourceName}} 上{{if .App}} {{.App}} 的{{end}}大日子"
  - name: sales-day
    then:
      title: '{{if gt .Quantity 0}}{{.QuantityFmt}} 笔销售{{if ne .Quantity 1}}{{end}} · {{.AmountFmt}}{{else if .Payload.downloads}}{{.Payload.downloads}} 次下载{{if ne (printf "%v" .Payload.downloads) "1"}}{{end}}{{else}}平静的一天{{end}}'
      subtitle: "{{if .App}}{{.App}} · {{end}}{{.SourceName}}"
  - name: flathub-record-day
    then:
      title: "Flathub 史上最佳一天"
      subtitle: "{{.App}} 的 {{.QuantityFmt}} 次安装"
  - name: flathub-installs
    then:
      title: "Flathub 上 {{.QuantityFmt}} 次安装"
      subtitle: "{{.App}}"
  - name: installs-day-record
    then:
      title: "{{.SourceName}} 上的最佳安装日"
      subtitle: "{{.QuantityFmt}} 次安装{{if .App}} · {{.App}}{{end}}"
  - name: installs-day
    then:
      title: "{{.SourceName}} 上 {{.QuantityFmt}} 次安装"
      subtitle: "{{.App}}"
  - name: settlement
    then:
      title: "新聚落：{{.Flag}} {{.Country}}"
      subtitle: "首位客户，来自 {{.Payload.via}}"
  - name: quest-complete-month
    then:
      title: "任务完成：{{.Payload.title}}"
      subtitle: "{{.Payload.detail}}"
  - name: quest-complete
    then:
      title: "任务完成：{{.Payload.title}}"
      subtitle: "{{.Payload.detail}}"
  - name: achievement-legendary
    then:
      title: "成就：{{.Payload.title}}"
      subtitle: "{{.Payload.description}}"
  - name: achievement-gold
    then:
      title: "成就：{{.Payload.title}}"
      subtitle: "{{.Payload.description}}"
  - name: achievement-silver
    then:
      title: "成就：{{.Payload.title}}"
      subtitle: "{{.Payload.description}}"
  - name: achievement
    then:
      title: "成就：{{.Payload.title}}"
      subtitle: "{{.Payload.description}}"
  - name: boss-spawn
    then:
      title: "Boss 出现：{{.Payload.name}}"
      subtitle: "{{.Payload.title}} · {{.Payload.detail}}"
  - name: boss-enrage
    then:
      title: "{{.Payload.name}} 陷入狂暴"
      subtitle: "{{.Payload.detail}}"
  - name: boss-slain-legendary
    then:
      title: "Boss 已击杀：{{.Payload.name}}"
      subtitle: "{{.Payload.detail}}"
  - name: boss-slain
    then:
      title: "Boss 已击杀：{{.Payload.name}}"
      subtitle: "{{.Payload.detail}}"
  - name: mystery-solved
    then:
      title: "谜团已解"
      subtitle: "{{.Payload.title}}"
  - name: github-release
    then:
      title: "版本已发布：{{.Payload.tag}}"
      subtitle: "{{.App}}"
  - name: github-star-milestone
    then:
      title: "{{.App}} 获得 {{.QuantityFmt}} 个星标"
      subtitle: "里程碑"
  - name: github-star
    then:
      title: "新的观星者"
      subtitle: "{{.App}}{{if .Payload.user}} · {{.Payload.user}}{{end}}"
  - name: github-fork
    then:
      title: "被 fork 了"
      subtitle: "{{.App}}"
  - name: github-issue-opened
    then:
      title: "新任务出现：#{{.Payload.number}}"
      subtitle: "{{.Payload.title}}"
  - name: github-issue-closed
    then:
      title: "Issue 已斩杀：#{{.Payload.number}}"
      subtitle: "{{.Payload.title}}"
  - name: github-pr-merged
    then:
      title: "PR 已合并：#{{.Payload.number}}"
      subtitle: "{{.Payload.title}}"
  - name: github-pr-opened
    then:
      title: "PR 已提交：#{{.Payload.number}}"
      subtitle: "{{.Payload.title}}"
  - name: webhook-legendary
    then:
      title: "{{.Payload.title}}"
      subtitle: "{{.Payload.subtitle}}"
  - name: webhook-epic
    then:
      title: "{{.Payload.title}}"
      subtitle: "{{.Payload.subtitle}}"
  - name: webhook-rare
    then:
      title: "{{.Payload.title}}"
      subtitle: "{{.Payload.subtitle}}"
  - name: webhook-uncommon
    then:
      title: "{{.Payload.title}}"
      subtitle: "{{.Payload.subtitle}}"
  - name: webhook-cursed
    then:
      title: "{{.Payload.title}}"
      subtitle: "{{.Payload.subtitle}}"
  - name: webhook-common
    then:
      title: "{{.Payload.title}}"
      subtitle: "{{.Payload.subtitle}}"
  - name: dev-cursed
    then:
      title: "一件诅咒掉落"
      subtitle: "合成测试事件"
  - name: dev-legendary
    then:
      title: "一件传说掉落"
      subtitle: "合成测试事件"
  - name: dev-epic
    then:
      title: "一件史诗掉落"
      subtitle: "合成测试事件"
  - name: dev-rare
    then:
      title: "一件稀有掉落"
      subtitle: "合成测试事件"
  - name: dev-uncommon
    then:
      title: "一件优秀掉落"
      subtitle: "合成测试事件"
  - name: dev-common
    then:
      title: "一件普通掉落"
      subtitle: "合成测试事件"
fallback:
  then:
    title: "{{.Source}} · {{.Kind}}"
    subtitle: "{{.App}}"
