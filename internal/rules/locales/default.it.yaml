# Loot default rules — Italian.
#
# GENERATED by tools/translate. Do not restructure this file by hand; the next
# run rewrites it from internal/rules/default.yaml.
#
# Editing a translation *is* supported, and is the intended way to fix a
# wrong one: an entry you rewrite is left exactly as you wrote it until the
# English title or subtitle it translates changes, at which point it is
# retranslated. (tools/translate records a hash of the English source in
# i18n.lock.json and only touches entries whose hash moved.)
#
# An overlay is the translatable half of default.yaml and nothing else. Each
# entry names a rule from default.yaml and gives its title and subtitle in this
# language; rarity, XP and matching are decisions rather than words and live in
# default.yaml alone.
#
# Every `{{…}}` expression is the English one, because it renders against the
# same event. Reordering them around the sentence is fine and often necessary;
# changing what one says is not.
#
# An entry applies only while the rule it names still has default.yaml's own
# wording. Copy default.yaml, rewrite a title, and that title stays yours in
# every language — see internal/rules/locales.go.

rules:
  - name: revenuecat-churn
    then:
      title: "Abbonato perso"
      subtitle: "{{.Kind}}{{if .App}} · {{.App}}{{end}}"
  - name: revenuecat-winback
    then:
      title: "Un abbonato torna"
      subtitle: "{{.App}}"
  - name: revenuecat-annual
    then:
      title: "Nuovo abbonato annuale"
      subtitle: "{{.AmountFmt}}{{if .App}} · {{.App}}{{end}}"
  - name: revenuecat-high-value
    then:
      title: "Nuovo abbonato"
      subtitle: "{{.AmountFmt}}{{if .App}} · {{.App}}{{end}}"
  - name: revenuecat-purchase
    then:
      title: "Nuovo abbonato"
      subtitle: "{{.AmountFmt}}{{if .App}} · {{.App}}{{end}}"
  - name: revenuecat-renewal
    then:
      title: "Rinnovo"
      subtitle: "{{.AmountFmt}}{{if .App}} · {{.App}}{{end}}"
  - name: revenuecat-test
    then:
      title: "Test del webhook RevenueCat ricevuto"
      subtitle: "il collegamento funziona{{if .App}} · {{.App}}{{end}}"
  - name: revenuecat-product-change
    then:
      title: "Piano cambiato"
      subtitle: "{{.App}}"
  - name: sales-day-record
    then:
      title: "Giorno migliore di sempre su {{.SourceName}}"
      subtitle: '{{if gt .Quantity 0}}{{.QuantityFmt}} vendit{{if ne .Quantity 1}}e{{else}}a{{end}} · {{.AmountFmt}}{{else if .Payload.downloads}}{{.Payload.downloads}} download{{if ne (printf "%v" .Payload.downloads) "1"}}{{end}}{{else}}un giorno tranquillo{{end}}{{if .App}} · {{.App}}{{end}}'
  - name: sales-day-big
    then:
      title: '{{if gt .Quantity 0}}{{.QuantityFmt}} vendit{{if ne .Quantity 1}}e{{else}}a{{end}} · {{.AmountFmt}}{{else if .Payload.downloads}}{{.Payload.downloads}} download{{if ne (printf "%v" .Payload.downloads) "1"}}{{end}}{{else}}un giorno tranquillo{{end}}'
      subtitle: "un gran giorno{{if .App}} per {{.App}}{{end}} su {{.SourceName}}"
  - name: sales-day
    then:
      title: '{{if gt .Quantity 0}}{{.QuantityFmt}} vendit{{if ne .Quantity 1}}e{{else}}a{{end}} · {{.AmountFmt}}{{else if .Payload.downloads}}{{.Payload.downloads}} download{{if ne (printf "%v" .Payload.downloads) "1"}}{{end}}{{else}}un giorno tranquillo{{end}}'
      subtitle: "{{if .App}}{{.App}} · {{end}}{{.SourceName}}"
  - name: flathub-record-day
    then:
      title: "Giorno migliore di sempre su Flathub"
      subtitle: "{{.QuantityFmt}} installazioni di {{.App}}"
  - name: flathub-installs
    then:
      title: "{{.QuantityFmt}} installazioni su Flathub"
      subtitle: "{{.App}}"
  - name: installs-day-record
    then:
      title: "Miglior giorno di installazioni su {{.SourceName}}"
      subtitle: "{{.QuantityFmt}} installazioni{{if .App}} · {{.App}}{{end}}"
  - name: installs-day
    then:
      title: "{{.QuantityFmt}} installazioni su {{.SourceName}}"
      subtitle: "{{.App}}"
  - name: settlement
    then:
      title: "Nuovo insediamento: {{.Flag}} {{.Country}}"
      subtitle: "primo cliente via {{.Payload.via}}"
  - name: quest-complete-month
    then:
      title: "Missione completata: {{.Payload.title}}"
      subtitle: "{{.Payload.detail}}"
  - name: quest-complete
    then:
      title: "Missione completata: {{.Payload.title}}"
      subtitle: "{{.Payload.detail}}"
  - name: achievement-legendary
    then:
      title: "Obiettivo: {{.Payload.title}}"
      subtitle: "{{.Payload.description}}"
  - name: achievement-gold
    then:
      title: "Obiettivo: {{.Payload.title}}"
      subtitle: "{{.Payload.description}}"
  - name: achievement-silver
    then:
      title: "Obiettivo: {{.Payload.title}}"
      subtitle: "{{.Payload.description}}"
  - name: achievement
    then:
      title: "Obiettivo: {{.Payload.title}}"
      subtitle: "{{.Payload.description}}"
  - name: boss-spawn
    then:
      title: "Appare un boss: {{.Payload.name}}"
      subtitle: "{{.Payload.title}} · {{.Payload.detail}}"
  - name: boss-enrage
    then:
      title: "{{.Payload.name}} si infuria"
      subtitle: "{{.Payload.detail}}"
  - name: boss-slain-legendary
    then:
      title: "Boss ucciso: {{.Payload.name}}"
      subtitle: "{{.Payload.detail}}"
  - name: boss-slain
    then:
      title: "Boss ucciso: {{.Payload.name}}"
      subtitle: "{{.Payload.detail}}"
  - name: mystery-solved
    then:
      title: "Mistero risolto"
      subtitle: "{{.Payload.title}}"
  - name: github-release
    then:
      title: "Release pubblicata: {{.Payload.tag}}"
      subtitle: "{{.App}}"
  - name: github-star-milestone
    then:
      title: "{{.QuantityFmt}} stelle su {{.App}}"
      subtitle: "traguardo"
  - name: github-star
    then:
      title: "Nuova stella"
      subtitle: "{{.App}}{{if .Payload.user}} · {{.Payload.user}}{{end}}"
  - name: github-fork
    then:
      title: "Nuovo fork"
      subtitle: "{{.App}}"
  - name: github-issue-opened
    then:
      title: "Appare una missione: #{{.Payload.number}}"
      subtitle: "{{.Payload.title}}"
  - name: github-issue-closed
    then:
      title: "Issue uccisa: #{{.Payload.number}}"
      subtitle: "{{.Payload.title}}"
  - name: github-pr-merged
    then:
      title: "PR unita: #{{.Payload.number}}"
      subtitle: "{{.Payload.title}}"
  - name: github-pr-opened
    then:
      title: "PR aperta: #{{.Payload.number}}"
      subtitle: "{{.Payload.title}}"
  - name: webhook-legendary
    then:
      title: "{{.Payload.title}}"
      subtitle: "{{.Payload.subtitle}}"
  - name: webhook-epic
    then:
      title: "{{.Payload.title}}"
      subtitle: "{{.Payload.subtitle}}"
  - name: webhook-rare
    then:
      title: "{{.Payload.title}}"
      subtitle: "{{.Payload.subtitle}}"
  - name: webhook-uncommon
    then:
      title: "{{.Payload.title}}"
      subtitle: "{{.Payload.subtitle}}"
  - name: webhook-cursed
    then:
      title: "{{.Payload.title}}"
      subtitle: "{{.Payload.subtitle}}"
  - name: webhook-common
    then:
      title: "{{.Payload.title}}"
      subtitle: "{{.Payload.subtitle}}"
  - name: dev-cursed
    then:
      title: "Un drop maledetto"
      subtitle: "evento di prova sintetico"
  - name: dev-legendary
    then:
      title: "Un drop leggendario"
      subtitle: "evento di prova sintetico"
  - name: dev-epic
    then:
      title: "Un drop epico"
      subtitle: "evento di prova sintetico"
  - name: dev-rare
    then:
      title: "Un drop raro"
      subtitle: "evento di prova sintetico"
  - name: dev-uncommon
    then:
      title: "Un drop non comune"
      subtitle: "evento di prova sintetico"
  - name: dev-common
    then:
      title: "Un drop comune"
      subtitle: "evento di prova sintetico"
fallback:
  then:
    title: "{{.Source}} · {{.Kind}}"
    subtitle: "{{.App}}"
