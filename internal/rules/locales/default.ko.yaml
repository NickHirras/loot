# Loot default rules — Korean.
#
# GENERATED by tools/translate. Do not restructure this file by hand; the next
# run rewrites it from internal/rules/default.yaml.
#
# Editing a translation *is* supported, and is the intended way to fix a
# wrong one: an entry you rewrite is left exactly as you wrote it until the
# English title or subtitle it translates changes, at which point it is
# retranslated. (tools/translate records a hash of the English source in
# i18n.lock.json and only touches entries whose hash moved.)
#
# An overlay is the translatable half of default.yaml and nothing else. Each
# entry names a rule from default.yaml and gives its title and subtitle in this
# language; rarity, XP and matching are decisions rather than words and live in
# default.yaml alone.
#
# Every `{{…}}` expression is the English one, because it renders against the
# same event. Reordering them around the sentence is fine and often necessary;
# changing what one says is not.
#
# An entry applies only while the rule it names still has default.yaml's own
# wording. Copy default.yaml, rewrite a title, and that title stays yours in
# every language — see internal/rules/locales.go.

rules:
  - name: revenuecat-churn
    then:
      title: "구독자 이탈"
      subtitle: "{{.Kind}}{{if .App}} · {{.App}}{{end}}"
  - name: revenuecat-winback
    then:
      title: "구독자가 돌아왔어요"
      subtitle: "{{.App}}"
  - name: revenuecat-annual
    then:
      title: "새 연간 구독자"
      subtitle: "{{.AmountFmt}}{{if .App}} · {{.App}}{{end}}"
  - name: revenuecat-high-value
    then:
      title: "새 구독자"
      subtitle: "{{.AmountFmt}}{{if .App}} · {{.App}}{{end}}"
  - name: revenuecat-purchase
    then:
      title: "새 구독자"
      subtitle: "{{.AmountFmt}}{{if .App}} · {{.App}}{{end}}"
  - name: revenuecat-renewal
    then:
      title: "갱신"
      subtitle: "{{.AmountFmt}}{{if .App}} · {{.App}}{{end}}"
  - name: revenuecat-test
    then:
      title: "RevenueCat 웹훅 테스트 수신"
      subtitle: "연결이 잘 됐어요{{if .App}} · {{.App}}{{end}}"
  - name: revenuecat-product-change
    then:
      title: "플랜 변경"
      subtitle: "{{.App}}"
  - name: sales-day-record
    then:
      title: "{{.SourceName}} 역대 최고의 날"
      subtitle: '{{if gt .Quantity 0}}판매 {{.QuantityFmt}}건{{if ne .Quantity 1}}{{end}} · {{.AmountFmt}}{{else if .Payload.downloads}}다운로드 {{.Payload.downloads}}회{{if ne (printf "%v" .Payload.downloads) "1"}}{{end}}{{else}}조용한 하루{{end}}{{if .App}} · {{.App}}{{end}}'
  - name: sales-day-big
    then:
      title: '{{if gt .Quantity 0}}판매 {{.QuantityFmt}}건{{if ne .Quantity 1}}{{end}} · {{.AmountFmt}}{{else if .Payload.downloads}}다운로드 {{.Payload.downloads}}회{{if ne (printf "%v" .Payload.downloads) "1"}}{{end}}{{else}}조용한 하루{{end}}'
      subtitle: "{{.SourceName}}에서{{if .App}} {{.App}}의{{end}} 큰 하루"
  - name: sales-day
    then:
      title: '{{if gt .Quantity 0}}판매 {{.QuantityFmt}}건{{if ne .Quantity 1}}{{end}} · {{.AmountFmt}}{{else if .Payload.downloads}}다운로드 {{.Payload.downloads}}회{{if ne (printf "%v" .Payload.downloads) "1"}}{{end}}{{else}}조용한 하루{{end}}'
      subtitle: "{{if .App}}{{.App}} · {{end}}{{.SourceName}}"
  - name: flathub-record-day
    then:
      title: "Flathub 역대 최고의 날"
      subtitle: "{{.App}} 설치 {{.QuantityFmt}}회"
  - name: flathub-installs
    then:
      title: "Flathub 설치 {{.QuantityFmt}}회"
      subtitle: "{{.App}}"
  - name: installs-day-record
    then:
      title: "{{.SourceName}} 최고 설치일"
      subtitle: "설치 {{.QuantityFmt}}회{{if .App}} · {{.App}}{{end}}"
  - name: installs-day
    then:
      title: "{{.SourceName}} 설치 {{.QuantityFmt}}회"
      subtitle: "{{.App}}"
  - name: settlement
    then:
      title: "새 정착지: {{.Flag}} {{.Country}}"
      subtitle: "{{.Payload.via}}를 통한 첫 고객"
  - name: quest-complete-month
    then:
      title: "퀘스트 완료: {{.Payload.title}}"
      subtitle: "{{.Payload.detail}}"
  - name: quest-complete
    then:
      title: "퀘스트 완료: {{.Payload.title}}"
      subtitle: "{{.Payload.detail}}"
  - name: achievement-legendary
    then:
      title: "업적: {{.Payload.title}}"
      subtitle: "{{.Payload.description}}"
  - name: achievement-gold
    then:
      title: "업적: {{.Payload.title}}"
      subtitle: "{{.Payload.description}}"
  - name: achievement-silver
    then:
      title: "업적: {{.Payload.title}}"
      subtitle: "{{.Payload.description}}"
  - name: achievement
    then:
      title: "업적: {{.Payload.title}}"
      subtitle: "{{.Payload.description}}"
  - name: boss-spawn
    then:
      title: "보스 등장: {{.Payload.name}}"
      subtitle: "{{.Payload.title}} · {{.Payload.detail}}"
  - name: boss-enrage
    then:
      title: "{{.Payload.name}} 격노"
      subtitle: "{{.Payload.detail}}"
  - name: boss-slain-legendary
    then:
      title: "보스 처치: {{.Payload.name}}"
      subtitle: "{{.Payload.detail}}"
  - name: boss-slain
    then:
      title: "보스 처치: {{.Payload.name}}"
      subtitle: "{{.Payload.detail}}"
  - name: mystery-solved
    then:
      title: "미스터리 해결"
      subtitle: "{{.Payload.title}}"
  - name: github-release
    then:
      title: "릴리스 출시: {{.Payload.tag}}"
      subtitle: "{{.App}}"
  - name: github-star-milestone
    then:
      title: "{{.App}} 스타 {{.QuantityFmt}}개"
      subtitle: "마일스톤"
  - name: github-star
    then:
      title: "새 스타"
      subtitle: "{{.App}}{{if .Payload.user}} · {{.Payload.user}}{{end}}"
  - name: github-fork
    then:
      title: "포크됨"
      subtitle: "{{.App}}"
  - name: github-issue-opened
    then:
      title: "퀘스트 등장: #{{.Payload.number}}"
      subtitle: "{{.Payload.title}}"
  - name: github-issue-closed
    then:
      title: "이슈 처치: #{{.Payload.number}}"
      subtitle: "{{.Payload.title}}"
  - name: github-pr-merged
    then:
      title: "PR 병합: #{{.Payload.number}}"
      subtitle: "{{.Payload.title}}"
  - name: github-pr-opened
    then:
      title: "PR 열림: #{{.Payload.number}}"
      subtitle: "{{.Payload.title}}"
  - name: webhook-legendary
    then:
      title: "{{.Payload.title}}"
      subtitle: "{{.Payload.subtitle}}"
  - name: webhook-epic
    then:
      title: "{{.Payload.title}}"
      subtitle: "{{.Payload.subtitle}}"
  - name: webhook-rare
    then:
      title: "{{.Payload.title}}"
      subtitle: "{{.Payload.subtitle}}"
  - name: webhook-uncommon
    then:
      title: "{{.Payload.title}}"
      subtitle: "{{.Payload.subtitle}}"
  - name: webhook-cursed
    then:
      title: "{{.Payload.title}}"
      subtitle: "{{.Payload.subtitle}}"
  - name: webhook-common
    then:
      title: "{{.Payload.title}}"
      subtitle: "{{.Payload.subtitle}}"
  - name: dev-cursed
    then:
      title: "저주 드롭"
      subtitle: "합성 테스트 이벤트"
  - name: dev-legendary
    then:
      title: "전설 드롭"
      subtitle: "합성 테스트 이벤트"
  - name: dev-epic
    then:
      title: "영웅 드롭"
      subtitle: "합성 테스트 이벤트"
  - name: dev-rare
    then:
      title: "희귀 드롭"
      subtitle: "합성 테스트 이벤트"
  - name: dev-uncommon
    then:
      title: "고급 드롭"
      subtitle: "합성 테스트 이벤트"
  - name: dev-common
    then:
      title: "일반 드롭"
      subtitle: "합성 테스트 이벤트"
fallback:
  then:
    title: "{{.Source}} · {{.Kind}}"
    subtitle: "{{.App}}"
